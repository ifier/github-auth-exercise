import { createMuiTheme } from "@material-ui/core";
import { blue } from '@material-ui/core/colors';

// Denys Morozov
// We can manage theme and override some styles
export const theme = createMuiTheme({
  palette: {
    primary: blue
  }
});
